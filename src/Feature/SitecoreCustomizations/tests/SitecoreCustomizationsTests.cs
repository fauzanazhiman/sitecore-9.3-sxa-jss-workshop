namespace CorporateSite.Feature.SitecoreCustomizations.Tests
{
    using System;

    using FluentAssertions;

    using NSubstitute;
    
    using Sitecore.FakeDb;

    using Xunit;

    /// <summary>
    /// Tests of SitecoreCustomizations Feature.
    /// </summary>
    public class SitecoreCustomizationsTests
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="SitecoreCustomizationsTests"/> class.
        /// </summary>
        public SitecoreCustomizationsTests()
        {
        }

        [Fact]
        //// e.g. IsNRICValid_IncorrectFirstCharacter_NotValid
        public void TestedMethodName_StateUnderTest_ExpectedBehavior()
        {
            //// Arrange
            //// Arrange test - put here logic required to setup current test (configure mocks, expected results, etc.).
            // NRICService service = new NRICService();
            // string nric = "A2439124H"

            //// Act
            //// Execute test - call here tested method.
            // bool result = service.IsNRICValid(nric);

            //// Assert
            //// Verify result - check result returned from tested method with expected result.
            // result.Should().BeFalse();
        }
    }
}
